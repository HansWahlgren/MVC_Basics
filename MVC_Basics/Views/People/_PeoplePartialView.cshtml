@{
        ViewData["Title"] = "_PeoplePartialView";
    }
@using MVC_Basics.Models
@model Person

    <table>

        @foreach (Person person in PersonViewModel.personList)
        {
            <tr>
                <td>Name: @person.Name,</td>
                <td>Phone number: @person.PhoneNumber,</td>
                <td>City: @person.City,</td>
                <td>
                    <form asp-action="RemovePerson" asp-controller="People" method="post">
                        <input type="hidden" name="personId" value=@person.Id />
                        <input type="submit" value="Remove" />
                    </form>
                </td>
            </tr>
        }
    </table>

            @*<tr>
                <td>Name: @Model.Name,</td>
                <td>Phone number: @Model.PhoneNumber,</td>
                <td>City: @Model.City,</td>
                <td>
                    <form asp-action="RemovePerson" asp-controller="People" method="post">
                        <input type="hidden" name="personId" value=@Model.Id />
                        <input type="submit" value="Remove" />
                    </form>
                </td>
            </tr>*@
        @*}*@
